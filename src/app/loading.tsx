export default function RouteLoading() {
  // Keep this minimal to avoid interfering with Next.js router auto-scroll.
  // Returning null prevents a full-screen fixed overlay during route transitions.
  return null;
}
